set(src ${CMAKE_CURRENT_SOURCE_DIR})

set(Run
        ${src}/Editor.cpp
        ${src}/EditorBody.cpp
        ${src}/EditorMenu.cpp
        ${src}/FileNamePrompt.cpp
        ${src}/NewMap.cpp
        ${src}/OpenMap.cpp
        ${src}/Run.cpp
        ${src}/SaveMap.cpp
        ${src}/Quit.cpp

        PARENT_SCOPE
        )
