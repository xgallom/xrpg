set(src ${CMAKE_CURRENT_SOURCE_DIR})

set(Graphics
        ${src}/Border.cpp
        ${src}/Character.cpp
        ${src}/Color.cpp
        ${src}/Coords.cpp
        ${src}/Input.cpp
        ${src}/Menu.cpp
        ${src}/OutputBuffer.cpp

        PARENT_SCOPE
        )