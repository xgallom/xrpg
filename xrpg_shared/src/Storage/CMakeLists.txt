set(src ${CMAKE_CURRENT_SOURCE_DIR})

set(Storage
        ${src}/Level.cpp
        ${src}/Save.cpp
        ${src}/Serialize.cpp
        ${src}/Settings.cpp
        ${src}/Storage.cpp

        PARENT_SCOPE
        )