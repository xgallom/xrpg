set(src ${CMAKE_CURRENT_SOURCE_DIR})

set(Util
        ${src}/AtomicBuffer.cpp
        ${src}/AtomicStream.cpp
        ${src}/AtomicStruct.cpp
        ${src}/Flag.cpp
        ${src}/Numeric.cpp
        ${src}/Result.cpp
        ${src}/String.cpp

        PARENT_SCOPE
        )