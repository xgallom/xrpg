set(src ${CMAKE_CURRENT_SOURCE_DIR})

set(Util
        ${src}/AtomicStructProvider.cpp
        ${src}/Flag.cpp
        ${src}/Numeric.cpp
        ${src}/Result.cpp
        ${src}/String.cpp

        PARENT_SCOPE
        )