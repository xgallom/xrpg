set(src ${CMAKE_CURRENT_SOURCE_DIR})

set(Run
        ${src}/Load.cpp
        ${src}/LoadGame.cpp
        ${src}/MainMenu.cpp
        ${src}/NewGame.cpp
        ${src}/Run.cpp

        PARENT_SCOPE
        )
