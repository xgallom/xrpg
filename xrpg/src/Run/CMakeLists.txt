set(src ${CMAKE_CURRENT_SOURCE_DIR})

set(Run
        ${src}/LoadGame.cpp
        ${src}/LoadLevel.cpp
        ${src}/MainMenu.cpp
        ${src}/NewGame.cpp
        ${src}/Run.cpp

        PARENT_SCOPE
        )
